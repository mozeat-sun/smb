@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Threads REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/ZooLogTargets.cmake")

# Set variables for backward compatibility
set(ZooLog_LIBRARIES ZooLog::zoo_log)
set(ZooLog_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")

check_required_components(ZooLog)

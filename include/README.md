# Public Headers

Top-level public header entry point.

Current public headers are still maintained in module folders (for example, `platform/inc`, `socket/inc`, `smb/inc`).

This directory is the long-term aggregation point for stable public API headers.
